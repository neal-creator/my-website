<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Professional Portfolio</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #2d3436;
            --accent: #0984e3;
            --bg: #ffffff;
            --text: #636e72;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
            color: var(--primary);
            background-color: var(--bg);
            scroll-behavior: smooth;
        }

        /* Navigation */
        nav {
            padding: 2rem 10%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            background: rgba(255,255,255,0.9);
            backdrop-filter: blur(10px);
            z-index: 1000;
        }

        .logo { font-weight: 600; font-size: 1.5rem; letter-spacing: -1px; }

        .nav-links a {
            text-decoration: none;
            color: var(--text);
            margin-left: 2rem;
            transition: color 0.3s;
        }

        .nav-links a:hover { color: var(--accent); }

        /* Hero Section */
        header {
            height: 80vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 0 10%;
        }

        h1 { font-size: 4rem; margin-bottom: 1rem; line-height: 1.1; }
        .accent-text { color: var(--accent); }

        p.subtitle { font-size: 1.25rem; color: var(--text); max-width: 600px; margin-bottom: 2rem; }

        .btn {
            display: inline-block;
            background: var(--primary);
            color: white;
            padding: 1rem 2.5rem;
            text-decoration: none;
            border-radius: 5px;
            transition: transform 0.2s;
        }

        .btn:hover { transform: translateY(-3px); background: var(--accent); }

        /* Content Section */
        section { padding: 5rem 10%; }
        h2 { font-size: 2rem; margin-bottom: 2rem; }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }

        .card {
            padding: 2rem;
            border: 1px solid #eee;
            border-radius: 10px;
            transition: border-color 0.3s;
        }

        .card:hover { border-color: var(--accent); }

        footer { padding: 5rem 10%; text-align: center; color: #b2bec3; font-size: 0.9rem; }

        @media (max-width: 768px) {
            h1 { font-size: 2.5rem; }
            nav { padding: 1.5rem 5%; }
            header { padding: 0 5%; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">BRANDNAME</div>
        <div class="nav-links">
            <a href="#about">About</a>
            <a href="#work">Work</a>
            <a href="mailto:hello@example.com">Contact</a>
        </div>
    </nav>

    <header>
        <h1>Building digital <br><span class="accent-text">experiences.</span></h1>
        <p class="subtitle">I'm a [Your Title] based in [Your Location]. I help people turn big ideas into functional, beautiful realities.</p>
        <div>
            <a href="#work" class="btn">View My Projects</a>
        </div>
    </header>

    <section id="work">
        <h2>Selected Work</h2>
        <div class="grid">
            <div class="card">
                <h3>Project One</h3>
                <p>A brief description of a cool thing you did for a client or yourself.</p>
            </div>
            <div class="card">
                <h3>Project Two</h3>
                <p>Focus on the results and the problem you solved here.</p>
            </div>
            <div class="card">
                <h3>Project Three</h3>
                <p>Another impressive piece of work that shows off your skills.</p>
            </div>
        </div>
    </section>

    <section id="about" style="background: #f9f9f9;">
        <h2>About Me</h2>
        <p style="max-width: 800px; font-size: 1.1rem; color: var(--text);">
            This is where you tell your story. Keep it brief, professional, and authentic. 
            Talk about what drives you and the unique value you bring to the table.
        </p>
    </section>

    <footer>
        &copy; 2026 BrandName. Built with Gemini.
    </footer>

</body>
</html>